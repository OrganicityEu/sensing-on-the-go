<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="eu.organicity.set.experiments">

    <application
        android:label="@string/app_name">

        <service
            android:name=".ExampleExperiment"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="organicity.intent.action.PICK_PLUGIN" />
                <action android:name="organicity.intent.action.PICK_DEVELOPER_EXPERIMENT" />
                <category android:name="organicity.intent.category.ADD_PLUGIN" />
            </intent-filter>
        </service>

    </application>

</manifest>
